[package]
name = "sage-infer"
version = "0.1.0"
edition.workspace = true

[dependencies]
rustc-hash = "2.1.2"
sage-stash = { path = "../sage-stash", features = ["salsa"] }
sage-ir = { path = "../sage-ir" }
salsa = "0.26.1"
smallvec = "1.15.1"

[dev-dependencies]
expect-test = "1.5.1"
