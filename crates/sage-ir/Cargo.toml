[package]
name = "sage-ir"
version = "0.1.0"
edition.workspace = true

[dependencies]
sage-stash = { path = "../sage-stash", features = ["salsa"] }
salsa = "0.26.1"
tree-sitter = "0.25"
tree-sitter-rust = "0.24"

[dev-dependencies]
expect-test = "1.5.1"
